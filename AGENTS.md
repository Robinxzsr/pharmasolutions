# AGENTS.md

See **[CLAUDE.md](./CLAUDE.md)** — it holds the conventions, the invariants
that look wrong but aren't, and the traps that have already cost time on this
codebase.

`README.md` covers the architecture itself.

Kept as a pointer rather than a copy so the two can't drift apart.
